{
  "name": "@repo/db",
  "version": "0.0.0",
  "main": "./src/index.ts",
  "type": "commonjs",
  "scripts": {
    "db:generate": "prisma generate",
    "db:push": "prisma db push --skip-generate",
    "build": "tsc -b"
  },
  "devDependencies": {
    "prisma": "^5.22.0"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0"
  }
}
