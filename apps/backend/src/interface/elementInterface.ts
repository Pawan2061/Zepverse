export interface addElementRequestBody {
  elementId: string;
  spaceId: string;
  x: number;
  y: number;
}

export interface SpaceElementRequestBody {
  elementId: string;
  spaceId: string;
  x: number;
  y: number;
}
